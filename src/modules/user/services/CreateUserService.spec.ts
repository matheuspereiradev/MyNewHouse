import {CreateUserService} from '@modules/user/services/CreateUserService';

describe('Test Service CreateUser',()=>{
    it('should be able to create a new user',()=>{
        
        // CreateUserService.e
        // name, email, birthDate, password, cpf, cnpj, street, houseNumber, district, complement, reference, income, phoneNumber, phoneNumber2, idCity
    });

    // it('not should be able to create a new user whith a wrong CPF',()=>{
    //     expect(1+2).toBe(3)
    // });
})